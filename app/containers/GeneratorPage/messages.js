import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GeneratorPage';

export default defineMessages({
  uploadImage: {
    id: `${scope}.uploadImage`,
    defaultMessage: 'Please select an image to upload',
  },
});
